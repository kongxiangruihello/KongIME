# KongIME 0.26 验证记录

日期：2026-09-26。

- 原生输入法、设置窗口和辅助工具编译通过。
- Python 回归：160 项，158 项通过，2 项缺少外部词库样本而跳过。包含真实 Rime 拼音检查、候选屏蔽、LevelDB 迁移、失败恢复和本机 HTTP 测试。
- 新增兼容性配置：默认语言不强制覆盖、按应用停用标点、非法参数拒绝、备份恢复与差异预览一致性。
- NSTextView：中文／英文空对插入、选区包裹、表情与多行 UTF-16 定位、已有右符号跳过；一个编辑事件中的撤销恢复原文本。保留英文单词中的撇号，中文后的引号可正常配对。
- 完整恢复报告能识别恢复后的配置变化；HTTP 测试确认恢复成功后仍是待应用状态，不把数据恢复视为部署成功。
- 更新检查：测试版需主动勾选；按版本号比较；忽略草稿、无安装包、异常版本和外部下载链接；网络失败可重试；返回内容有大小上限；接口有会话校验。
- 隔离数据的设置界面：搜索“兼容”可定位；微信规则保存显示“跟随客户端默认 · 成对标点已停用”；恢复顶部显示实际安装和运行版本不一致提示；手动更新能连接 GitHub 并正确显示无正式版。

## 尚待真实设备验证

当前本机安装并运行的是 0.24.0。未替换正在使用的输入法，未注销系统。

| 应用 | 简拼／全拼 | 中英文切换 | 屏幕边缘候选 | 标点包裹／跳过／撤销 |
| --- | --- | --- | --- | --- |
| 微信 | 待实测 | 待实测 | 待实测 | 待实测 |
| Microsoft Word | 待实测 | 待实测 | 待实测 | 待实测 |
| Google Chrome | 待实测 | 待实测 | 待实测 | 待实测 |
| 备忘录 | 待实测 | 待实测 | 待实测 | 待实测 |

检查方式：安装本版并重新登录，分别输入 nihao、nh、ni'h；切换中英文；在窗口和屏幕边缘输入长词；包裹中文、表情、多行选区，输入已有右符号，然后撤销。遇到标点不兼容，可仅停用该应用的成对标点。

自动引擎检查与 NSTextView 通过不能代替上表，也不能证明远端应用已加载新配置。两台物理 Mac 的 iCloud 恢复仍待现场验证。
