# KongIME 0.25 验证记录

日期：2026-09-25。

- Python 回归 153 项：151 通过，2 项因缺少外部／私有词库样本跳过。覆盖旧功能与新增设置、屏蔽、HTTP 预览鉴权、完整迁移。原生客户端与全部辅助工具编译通过。
- Rime 真实引擎：nihao、nh、ni'h 的你好可屏蔽；同时有排序规则时仍屏蔽，zg 的中国不受影响；取消屏蔽后恢复。
- 真实 LevelDB：包含词库、动态短语、屏蔽偏好和学习次数的完整备份往返、恢复前备份可再次恢复、空学习词库恢复。
- 故障注入：替换学习数据库后写入报告失败，配置和学习词频同时回退；模拟进程中断，重新启动维护程序完成恢复。所有测试只使用临时数据。
- 预览：校验摘要不一致、无效学习次数、配置变化、预览超时均拒绝恢复。
- 原生 NSTextView：中英文括号／引号插入，光标留在两字符之间，没有遗留拼音组合；空对退格删除；含 emoji 的 UTF-16 定位；英文词内撇号不自动配对。
- 设置页面：搜索“成对”定位输入习惯；中英文标点开关、切换提示保存成功；旧客户端下完整迁移按钮禁用并显示升级说明。

限制：尚未安装 0.25，因此跨应用中英文提示、浏览器／第三方编辑器的成对标点未实际验证；两台物理 Mac 的迁移尚未验证。此处的数据库往返检查不能替代这些现场测试。
